services:
  hashcards:
    build: .
    ports:
      - "3001:3000"
    volumes:
      - ./config.json:/app/config.json:ro
      - ./example:/app/data
      - ./extras/menu.sh:/app/menu.sh:ro # optional
    restart: unless-stopped
