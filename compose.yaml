services:
  my-mind:
    image: registry.internal/my-mind:latest
    #ports:
    #  - "3001:3000"
    volumes:
      - data:/app/maps
    restart: unless-stopped
    environment:
      PORT: 3000
      TZ: Asia/Tokyo
    container_name: my-mind
    networks:
      - traefik_br
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.my-mind.rule=Host(`mm.notes.internal`)"
      - "traefik.http.routers.my-mind.middlewares=authelia@docker"
      - "diun.enable=true"

networks:
  traefik_br:
    external: true
volumes:
  data:
